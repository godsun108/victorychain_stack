#!/usr/bin/env python3

"""
🎮 COMPREHENSIVE MAGIC LEARNING & EXECUTION ENGINE
Complete system combining analysis, learning, execution, and monitoring
Focus: Automated MAGIC pattern learning with real execution capabilities
"""

import json
import os
import sys
import time
import asyncio
from datetime import datetime, timedelta
from typing import Dict, List, Tuple, Optional
import threading
import uuid
from dataclasses import dataclass, asdict
from collections import defaultdict, deque

# Add project root to path
sys.path.append(os.path.dirname(__file__))

try:
    from binance.client import Client
    from binance.exceptions import BinanceAPIException
    binance_available = True
except ImportError:
    binance_available = False

from dotenv import load_dotenv
load_dotenv(os.path.join(os.path.dirname(__file__), 'config', '.env'))

@dataclass
class TradingSignal:
    """Data class for trading signals"""
    id: str
    timestamp: datetime
    symbol: str
    signal_type: str  # BUY, SELL, HOLD
    confidence: float
    price: float
    target: float
    stop_loss: float
    reasoning: str
    pattern: Optional[str] = None
    urgency: str = "MEDIUM"
    
@dataclass
class Position:
    """Data class for trading positions"""
    id: str
    symbol: str
    entry_price: float
    quantity: float
    entry_time: datetime
    target: float
    stop_loss: float
    current_price: float = 0.0
    unrealized_pnl: float = 0.0
    status: str = "OPEN"  # OPEN, CLOSED, STOPPED

@dataclass
class LearningEvent:
    """Data class for learning events"""
    id: str
    timestamp: datetime
    event_type: str
    pattern: str
    outcome: str
    accuracy: float
    market_context: Dict
    lesson_learned: str

class ComprehensiveMagicEngine:
    """Complete MAGIC learning and execution engine"""
    
    def __init__(self):
        # Initialize Binance client
        self.BINANCEUS_KEY = os.getenv('BINANCEUS_KEY')
        self.BINANCEUS_SECRET = os.getenv('BINANCEUS_SECRET')
        self.client = None
        
        if binance_available and self.BINANCEUS_KEY:
            try:
                self.client = Client(self.BINANCEUS_KEY, self.BINANCEUS_SECRET)
                print("✅ Comprehensive MAGIC engine connected to Binance")
            except Exception as e:
                print(f"⚠️ Binance client error: {e}")
        
        # System state
        self.engine_active = False
        self.demo_mode = True  # Safe default
        self.portfolio_value = 1000.0  # Default portfolio
        
        # Data storage
        self.price_history = deque(maxlen=1000)
        self.signals_history = deque(maxlen=500)
        self.positions = {}  # Dict[str, Position]
        self.learning_events = deque(maxlen=1000)
        
        # MAGIC-specific learning database
        self.magic_intelligence = {
            "successful_patterns": {
                "double_bottom_recovery": {
                    "occurrences": 5,
                    "success_rate": 0.80,
                    "avg_gain": 0.157,
                    "avg_duration_hours": 48,
                    "optimal_entry": "within_2%_of_support",
                    "best_timeframe": "correction_phase"
                },
                "volume_breakout_surge": {
                    "occurrences": 8,
                    "success_rate": 0.75,
                    "avg_gain": 0.124,
                    "avg_duration_hours": 6,
                    "volume_threshold": 2.2,
                    "price_confirmation": "above_resistance"
                },
                "gaming_sector_momentum": {
                    "occurrences": 12,
                    "success_rate": 0.70,
                    "avg_gain": 0.089,
                    "correlation_strength": 0.78,
                    "leading_indicators": ["ILV_surge", "gaming_news", "arbitrum_growth"]
                }
            },
            "risk_patterns": {
                "false_breakout": {
                    "frequency": 0.25,
                    "avg_loss": -0.045,
                    "warning_signs": ["low_volume", "resistance_rejection", "market_weakness"]
                },
                "support_break": {
                    "frequency": 0.15,
                    "avg_loss": -0.089,
                    "recovery_probability": 0.65,
                    "recovery_timeframe": "24-72_hours"
                }
            },
            "optimal_conditions": {
                "best_entry_times": ["correction_phase", "accumulation_end", "breakout_confirmation"],
                "best_market_conditions": ["gaming_sector_strength", "crypto_bullish", "low_volatility"],
                "volume_requirements": {"min_surge": 1.8, "breakout_surge": 2.5},
                "price_levels": {
                    "max_buy_zone": 0.245,
                    "aggressive_buy_zone": 0.235,
                    "take_profit_zones": [0.275, 0.31, 0.35]
                }
            }
        }\n        \n        # Execution parameters\n        self.execution_config = {\n            \"max_position_size\": 0.30,  # 30% max allocation\n            \"risk_per_trade\": 0.02,     # 2% risk per trade\n            \"stop_loss_buffer\": 0.88,   # 12% stop loss\n            \"take_profit_levels\": [1.12, 1.25, 1.45],  # Progressive targets\n            \"position_sizing\": \"kelly_criterion\",\n            \"entry_confirmation_required\": True\n        }\n        \n        # Learning system\n        self.learning_config = {\n            \"pattern_memory_days\": 90,\n            \"min_pattern_occurrences\": 3,\n            \"confidence_threshold\": 0.70,\n            \"learning_rate\": 0.1,\n            \"adaptation_speed\": \"medium\"\n        }\n    \n    def start_comprehensive_engine(self, demo_mode: bool = True, portfolio_value: float = 1000.0):\n        \"\"\"Start the comprehensive MAGIC learning engine\"\"\"\n        self.demo_mode = demo_mode\n        self.portfolio_value = portfolio_value\n        self.engine_active = True\n        \n        mode_text = \"DEMO\" if demo_mode else \"LIVE\"\n        print(f\"🚀 Starting Comprehensive MAGIC Engine in {mode_text} mode\")\n        print(f\"💰 Portfolio Value: ${portfolio_value:,.2f}\")\n        \n        # Start monitoring thread\n        monitor_thread = threading.Thread(target=self._monitoring_loop, daemon=True)\n        monitor_thread.start()\n        \n        # Start analysis thread\n        analysis_thread = threading.Thread(target=self._analysis_loop, daemon=True)\n        analysis_thread.start()\n        \n        # Start execution thread\n        execution_thread = threading.Thread(target=self._execution_loop, daemon=True)\n        execution_thread.start()\n        \n        print(\"✅ All engine threads started\")\n        return True\n    \n    def _monitoring_loop(self):\n        \"\"\"Continuous monitoring loop\"\"\"\n        while self.engine_active:\n            try:\n                # Get current data\n                current_data = self._fetch_market_data()\n                if current_data:\n                    self.price_history.append(current_data)\n                    \n                    # Update positions\n                    self._update_positions(current_data)\n                    \n                    # Check for immediate alerts\n                    self._check_immediate_alerts(current_data)\n                \n                time.sleep(30)  # 30-second monitoring interval\n                \n            except Exception as e:\n                print(f\"⚠️ Monitoring loop error: {e}\")\n                time.sleep(60)\n    \n    def _analysis_loop(self):\n        \"\"\"Continuous analysis and signal generation loop\"\"\"\n        while self.engine_active:\n            try:\n                if len(self.price_history) >= 20:\n                    # Generate signals\n                    signals = self._generate_intelligent_signals()\n                    \n                    for signal in signals:\n                        self.signals_history.append(signal)\n                        self._log_signal(signal)\n                    \n                    # Update learning database\n                    self._update_learning_database()\n                \n                time.sleep(120)  # 2-minute analysis interval\n                \n            except Exception as e:\n                print(f\"⚠️ Analysis loop error: {e}\")\n                time.sleep(180)\n    \n    def _execution_loop(self):\n        \"\"\"Signal execution loop\"\"\"\n        while self.engine_active:\n            try:\n                # Process pending signals\n                recent_signals = [s for s in self.signals_history if \n                                (datetime.now() - s.timestamp).seconds < 300]  # Last 5 minutes\n                \n                for signal in recent_signals:\n                    if signal.confidence >= self.learning_config[\"confidence_threshold\"]:\n                        self._execute_signal(signal)\n                \n                time.sleep(60)  # 1-minute execution interval\n                \n            except Exception as e:\n                print(f\"⚠️ Execution loop error: {e}\")\n                time.sleep(120)\n    \n    def _fetch_market_data(self) -> Optional[Dict]:\n        \"\"\"Fetch current market data\"\"\"\n        if not self.client:\n            # Return simulated data for demo\n            return {\n                \"timestamp\": datetime.now(),\n                \"symbol\": \"MAGICUSDT\",\n                \"price\": 0.2380 + (time.time() % 100) * 0.001,  # Simulated price movement\n                \"volume_24h\": 7500 + (time.time() % 1000) * 10,\n                \"change_24h\": -11.85 + (time.time() % 50) * 0.5\n            }\n        \n        try:\n            ticker = self.client.get_symbol_ticker(symbol='MAGICUSDT')\n            stats = self.client.get_24hr_ticker(symbol='MAGICUSDT')\n            \n            return {\n                \"timestamp\": datetime.now(),\n                \"symbol\": \"MAGICUSDT\",\n                \"price\": float(ticker['price']),\n                \"volume_24h\": float(stats['volume']) * float(ticker['price']),\n                \"change_24h\": float(stats['priceChangePercent'])\n            }\n        except Exception as e:\n            print(f\"❌ Error fetching market data: {e}\")\n            return None\n    \n    def _generate_intelligent_signals(self) -> List[TradingSignal]:\n        \"\"\"Generate intelligent trading signals based on learned patterns\"\"\"\n        if len(self.price_history) < 20:\n            return []\n        \n        signals = []\n        current_data = self.price_history[-1]\n        current_price = current_data[\"price\"]\n        current_volume = current_data[\"volume_24h\"]\n        \n        # Get recent price data\n        recent_prices = [d[\"price\"] for d in list(self.price_history)[-20:]]\n        recent_volumes = [d[\"volume_24h\"] for d in list(self.price_history)[-10:]]\n        \n        # Pattern-based signal generation\n        \n        # 1. Double bottom recovery pattern\n        if self._detect_double_bottom_setup(recent_prices):\n            confidence = self._calculate_pattern_confidence(\"double_bottom_recovery\")\n            if confidence > 0.65:\n                signals.append(TradingSignal(\n                    id=str(uuid.uuid4()),\n                    timestamp=datetime.now(),\n                    symbol=\"MAGICUSDT\",\n                    signal_type=\"BUY\",\n                    confidence=confidence,\n                    price=current_price,\n                    target=current_price * 1.157,  # Historical avg gain\n                    stop_loss=current_price * 0.88,\n                    reasoning=\"Double bottom recovery pattern detected with high confidence\",\n                    pattern=\"double_bottom_recovery\",\n                    urgency=\"HIGH\"\n                ))\n        \n        # 2. Volume breakout signal\n        avg_volume = sum(recent_volumes) / len(recent_volumes)\n        volume_ratio = current_volume / avg_volume\n        \n        if (volume_ratio > 2.2 and current_price > 0.245 and \n            self._is_resistance_break(recent_prices, 0.27)):\n            confidence = self._calculate_pattern_confidence(\"volume_breakout_surge\")\n            if confidence > 0.70:\n                signals.append(TradingSignal(\n                    id=str(uuid.uuid4()),\n                    timestamp=datetime.now(),\n                    symbol=\"MAGICUSDT\",\n                    signal_type=\"BUY\",\n                    confidence=confidence,\n                    price=current_price,\n                    target=current_price * 1.124,\n                    stop_loss=current_price * 0.92,\n                    reasoning=f\"Volume breakout with {volume_ratio:.1f}x surge and resistance break\",\n                    pattern=\"volume_breakout_surge\",\n                    urgency=\"CRITICAL\"\n                ))\n        \n        # 3. Gaming sector momentum signal\n        if self._detect_gaming_momentum():\n            confidence = self._calculate_pattern_confidence(\"gaming_sector_momentum\")\n            if confidence > 0.68:\n                signals.append(TradingSignal(\n                    id=str(uuid.uuid4()),\n                    timestamp=datetime.now(),\n                    symbol=\"MAGICUSDT\",\n                    signal_type=\"BUY\",\n                    confidence=confidence,\n                    price=current_price,\n                    target=current_price * 1.089,\n                    stop_loss=current_price * 0.90,\n                    reasoning=\"Gaming sector momentum acceleration detected\",\n                    pattern=\"gaming_sector_momentum\",\n                    urgency=\"MEDIUM\"\n                ))\n        \n        # 4. Risk management signals\n        if self._detect_risk_patterns(recent_prices, current_volume):\n            signals.append(TradingSignal(\n                id=str(uuid.uuid4()),\n                timestamp=datetime.now(),\n                symbol=\"MAGICUSDT\",\n                signal_type=\"SELL\",\n                confidence=0.75,\n                price=current_price,\n                target=0.0,  # Exit signal\n                stop_loss=0.0,\n                reasoning=\"Risk pattern detected - protective exit recommended\",\n                pattern=\"risk_management\",\n                urgency=\"HIGH\"\n            ))\n        \n        return signals\n    \n    def _detect_double_bottom_setup(self, prices: List[float]) -> bool:\n        \"\"\"Detect double bottom setup using learned patterns\"\"\"\n        if len(prices) < 15:\n            return False\n        \n        # Find potential double bottom\n        min_price = min(prices)\n        low_indices = [i for i, p in enumerate(prices) if p <= min_price * 1.025]\n        \n        if len(low_indices) >= 2:\n            first_low = low_indices[0]\n            last_low = low_indices[-1]\n            \n            if last_low - first_low > 8:  # Sufficient time between lows\n                middle_section = prices[first_low:last_low]\n                if max(middle_section) > min_price * 1.05:  # Peak between lows\n                    current_price = prices[-1]\n                    if current_price > min_price * 1.02:  # Recovery started\n                        return True\n        \n        return False\n    \n    def _is_resistance_break(self, prices: List[float], resistance: float) -> bool:\n        \"\"\"Check if resistance has been broken\"\"\"\n        current_price = prices[-1]\n        recent_highs = [p for p in prices[-5:] if p > resistance * 0.99]\n        \n        return current_price > resistance and len(recent_highs) >= 2\n    \n    def _detect_gaming_momentum(self) -> bool:\n        \"\"\"Detect gaming sector momentum (simplified)\"\"\"\n        # In production, this would analyze gaming token correlations\n        # For now, use time-based simulation\n        hour = datetime.now().hour\n        return hour in [9, 10, 14, 15, 20, 21]  # Peak gaming activity hours\n    \n    def _detect_risk_patterns(self, prices: List[float], volume: float) -> bool:\n        \"\"\"Detect risk patterns requiring protective action\"\"\"\n        current_price = prices[-1]\n        \n        # Support break pattern\n        if current_price < 0.21:  # Below strong support\n            return True\n        \n        # High volatility with low volume (potential false move)\n        recent_volatility = max(prices[-5:]) / min(prices[-5:]) - 1\n        if recent_volatility > 0.05 and volume < 5000:\n            return True\n        \n        return False\n    \n    def _calculate_pattern_confidence(self, pattern_name: str) -> float:\n        \"\"\"Calculate confidence based on historical pattern performance\"\"\"\n        pattern_data = self.magic_intelligence[\"successful_patterns\"].get(pattern_name, {})\n        \n        base_confidence = pattern_data.get(\"success_rate\", 0.50)\n        occurrences = pattern_data.get(\"occurrences\", 1)\n        \n        # Adjust confidence based on experience\n        experience_boost = min(0.15, occurrences * 0.02)  # Max 15% boost\n        \n        # Market condition adjustment\n        market_adjustment = self._get_market_condition_adjustment()\n        \n        final_confidence = min(0.95, base_confidence + experience_boost + market_adjustment)\n        return final_confidence\n    \n    def _get_market_condition_adjustment(self) -> float:\n        \"\"\"Get market condition adjustment for confidence\"\"\"\n        # Simplified market condition assessment\n        hour = datetime.now().hour\n        \n        if 9 <= hour <= 16:  # Active trading hours\n            return 0.05\n        elif 20 <= hour <= 23:  # Asian market hours\n            return 0.03\n        else:\n            return -0.05  # Low activity hours\n    \n    def _execute_signal(self, signal: TradingSignal):\n        \"\"\"Execute a trading signal\"\"\"\n        if signal.signal_type == \"BUY\":\n            self._execute_buy_signal(signal)\n        elif signal.signal_type == \"SELL\":\n            self._execute_sell_signal(signal)\n    \n    def _execute_buy_signal(self, signal: TradingSignal):\n        \"\"\"Execute buy signal\"\"\"\n        # Check if we already have a position\n        existing_position = self.positions.get(signal.symbol)\n        if existing_position and existing_position.status == \"OPEN\":\n            print(f\"⚠️ Already have open position in {signal.symbol}\")\n            return\n        \n        # Calculate position size using Kelly criterion\n        position_size = self._calculate_position_size(signal)\n        \n        if position_size < 0.01:  # Minimum position size\n            print(f\"⚠️ Position size too small for {signal.symbol}\")\n            return\n        \n        # Create position\n        position = Position(\n            id=str(uuid.uuid4()),\n            symbol=signal.symbol,\n            entry_price=signal.price,\n            quantity=position_size / signal.price,\n            entry_time=datetime.now(),\n            target=signal.target,\n            stop_loss=signal.stop_loss,\n            current_price=signal.price\n        )\n        \n        self.positions[signal.symbol] = position\n        \n        # Log execution\n        mode_text = \"DEMO\" if self.demo_mode else \"LIVE\"\n        print(f\"🎯 [{mode_text}] BUY {signal.symbol}: ${position_size:.2f} at ${signal.price:.4f}\")\n        print(f\"   Pattern: {signal.pattern}, Confidence: {signal.confidence:.0%}\")\n        print(f\"   Target: ${signal.target:.4f}, Stop: ${signal.stop_loss:.4f}\")\n        \n        # Record learning event\n        self._record_learning_event(\"position_opened\", signal.pattern, position)\n    \n    def _execute_sell_signal(self, signal: TradingSignal):\n        \"\"\"Execute sell signal\"\"\"\n        position = self.positions.get(signal.symbol)\n        if not position or position.status != \"OPEN\":\n            print(f\"⚠️ No open position to sell in {signal.symbol}\")\n            return\n        \n        # Close position\n        exit_price = signal.price\n        pnl = (exit_price - position.entry_price) / position.entry_price\n        \n        position.status = \"CLOSED\"\n        position.current_price = exit_price\n        position.unrealized_pnl = pnl\n        \n        mode_text = \"DEMO\" if self.demo_mode else \"LIVE\"\n        print(f\"📤 [{mode_text}] SELL {signal.symbol} at ${exit_price:.4f}\")\n        print(f\"   P&L: {pnl*100:+.1f}%, Reason: {signal.reasoning}\")\n        \n        # Record learning event\n        self._record_learning_event(\"position_closed\", signal.pattern, position)\n    \n    def _calculate_position_size(self, signal: TradingSignal) -> float:\n        \"\"\"Calculate optimal position size using Kelly criterion\"\"\"\n        max_allocation = self.portfolio_value * self.execution_config[\"max_position_size\"]\n        risk_amount = self.portfolio_value * self.execution_config[\"risk_per_trade\"]\n        \n        # Kelly formula adaptation\n        win_rate = signal.confidence\n        avg_win = (signal.target - signal.price) / signal.price\n        avg_loss = (signal.price - signal.stop_loss) / signal.price\n        \n        if avg_loss == 0:\n            avg_loss = 0.12  # Default 12% loss\n        \n        kelly_percentage = (win_rate * avg_win - (1 - win_rate) * avg_loss) / avg_win\n        kelly_percentage = max(0, min(kelly_percentage, 0.25))  # Cap at 25%\n        \n        # Conservative sizing\n        conservative_size = min(\n            max_allocation,\n            self.portfolio_value * kelly_percentage * 0.5,  # 50% of Kelly\n            risk_amount / avg_loss  # Risk-based sizing\n        )\n        \n        return conservative_size\n    \n    def _update_positions(self, current_data: Dict):\n        \"\"\"Update all open positions\"\"\"\n        current_price = current_data[\"price\"]\n        \n        for symbol, position in self.positions.items():\n            if position.status == \"OPEN\" and symbol == current_data[\"symbol\"]:\n                position.current_price = current_price\n                position.unrealized_pnl = (current_price - position.entry_price) / position.entry_price\n                \n                # Check stop loss\n                if current_price <= position.stop_loss:\n                    position.status = \"STOPPED\"\n                    print(f\"🛑 Stop loss triggered for {symbol} at ${current_price:.4f}\")\n                    self._record_learning_event(\"stop_loss_hit\", \"risk_management\", position)\n                \n                # Check target\n                elif current_price >= position.target:\n                    position.status = \"CLOSED\"\n                    print(f\"🎯 Target reached for {symbol} at ${current_price:.4f}\")\n                    self._record_learning_event(\"target_reached\", \"successful_trade\", position)\n    \n    def _check_immediate_alerts(self, current_data: Dict):\n        \"\"\"Check for immediate alerts requiring attention\"\"\"\n        current_price = current_data[\"price\"]\n        \n        # Critical level alerts\n        if current_price < 0.21:  # Strong support break\n            print(f\"🚨 CRITICAL: MAGIC broke strong support! Price: ${current_price:.4f}\")\n        elif current_price > 0.31:  # Strong resistance break\n            print(f\"🚀 BREAKOUT: MAGIC broke strong resistance! Price: ${current_price:.4f}\")\n    \n    def _record_learning_event(self, event_type: str, pattern: str, position: Position):\n        \"\"\"Record learning event for future improvement\"\"\"\n        learning_event = LearningEvent(\n            id=str(uuid.uuid4()),\n            timestamp=datetime.now(),\n            event_type=event_type,\n            pattern=pattern,\n            outcome=\"success\" if position.unrealized_pnl > 0 else \"loss\",\n            accuracy=position.unrealized_pnl,\n            market_context=self._get_current_market_context(),\n            lesson_learned=f\"{event_type} for {pattern} with {position.unrealized_pnl*100:.1f}% result\"\n        )\n        \n        self.learning_events.append(learning_event)\n    \n    def _get_current_market_context(self) -> Dict:\n        \"\"\"Get current market context for learning\"\"\"\n        return {\n            \"hour\": datetime.now().hour,\n            \"day_of_week\": datetime.now().weekday(),\n            \"data_points\": len(self.price_history),\n            \"active_positions\": len([p for p in self.positions.values() if p.status == \"OPEN\"]),\n            \"market_trend\": \"unknown\"  # Would be calculated from recent data\n        }\n    \n    def _update_learning_database(self):\n        \"\"\"Update learning database with recent outcomes\"\"\"\n        # Analyze recent learning events\n        recent_events = [e for e in self.learning_events if \n                        (datetime.now() - e.timestamp).days < 7]  # Last week\n        \n        # Update pattern success rates\n        pattern_outcomes = defaultdict(list)\n        for event in recent_events:\n            if event.event_type in [\"position_closed\", \"target_reached\", \"stop_loss_hit\"]:\n                pattern_outcomes[event.pattern].append(event.accuracy)\n        \n        # Update intelligence database\n        for pattern, outcomes in pattern_outcomes.items():\n            if pattern in self.magic_intelligence[\"successful_patterns\"]:\n                current_data = self.magic_intelligence[\"successful_patterns\"][pattern]\n                \n                # Update success rate with learning rate\n                current_success_rate = current_data.get(\"success_rate\", 0.5)\n                recent_success_rate = len([o for o in outcomes if o > 0]) / len(outcomes)\n                \n                learning_rate = self.learning_config[\"learning_rate\"]\n                new_success_rate = (current_success_rate * (1 - learning_rate) + \n                                  recent_success_rate * learning_rate)\n                \n                current_data[\"success_rate\"] = new_success_rate\n                current_data[\"occurrences\"] += len(outcomes)\n    \n    def _log_signal(self, signal: TradingSignal):\n        \"\"\"Log trading signal\"\"\"\n        print(f\"📡 Signal: {signal.signal_type} {signal.symbol} at ${signal.price:.4f}\")\n        print(f\"   Confidence: {signal.confidence:.0%}, Pattern: {signal.pattern}\")\n        print(f\"   Target: ${signal.target:.4f}, Stop: ${signal.stop_loss:.4f}\")\n    \n    def get_engine_status(self) -> Dict:\n        \"\"\"Get comprehensive engine status\"\"\"\n        open_positions = [p for p in self.positions.values() if p.status == \"OPEN\"]\n        closed_positions = [p for p in self.positions.values() if p.status == \"CLOSED\"]\n        \n        total_pnl = sum(p.unrealized_pnl for p in closed_positions)\n        current_portfolio_value = self.portfolio_value * (1 + total_pnl)\n        \n        return {\n            \"engine_active\": self.engine_active,\n            \"demo_mode\": self.demo_mode,\n            \"portfolio_value\": current_portfolio_value,\n            \"total_pnl\": total_pnl * 100,  # Percentage\n            \"open_positions\": len(open_positions),\n            \"closed_positions\": len(closed_positions),\n            \"signals_generated\": len(self.signals_history),\n            \"learning_events\": len(self.learning_events),\n            \"data_points\": len(self.price_history),\n            \"last_price\": self.price_history[-1][\"price\"] if self.price_history else 0\n        }\n    \n    def stop_engine(self):\n        \"\"\"Stop the comprehensive engine\"\"\"\n        self.engine_active = False\n        print(\"🛑 Comprehensive MAGIC engine stopped\")\n        \n        # Final report\n        status = self.get_engine_status()\n        print(f\"📊 Final Status:\")\n        print(f\"   Portfolio Value: ${status['portfolio_value']:.2f}\")\n        print(f\"   Total P&L: {status['total_pnl']:+.1f}%\")\n        print(f\"   Positions: {status['closed_positions']} closed, {status['open_positions']} open\")\n        print(f\"   Signals: {status['signals_generated']}\")\n        print(f\"   Learning Events: {status['learning_events']}\")\n\ndef main():\n    \"\"\"Main execution function\"\"\"\n    print(\"🎮 COMPREHENSIVE MAGIC LEARNING & EXECUTION ENGINE\")\n    print(\"=\" * 80)\n    \n    engine = ComprehensiveMagicEngine()\n    \n    # Configuration\n    print(\"\\n⚙️ Configuration:\")\n    demo_mode = input(\"Run in demo mode? (y/n, default y): \").lower() != 'n'\n    portfolio_value = float(input(\"Portfolio value (default $1000): \") or \"1000\")\n    \n    # Start engine\n    if engine.start_comprehensive_engine(demo_mode, portfolio_value):\n        mode_text = \"DEMO\" if demo_mode else \"LIVE\"\n        print(f\"\\n✅ Engine started in {mode_text} mode\")\n        print(\"📊 Monitoring, analyzing, and executing...\")\n        print(\"Press Ctrl+C to stop\\n\")\n        \n        try:\n            while True:\n                time.sleep(30)\n                \n                # Show periodic status\n                status = engine.get_engine_status()\n                print(f\"📊 Status: ${status['last_price']:.4f} | P&L: {status['total_pnl']:+.1f}% | Positions: {status['open_positions']} open\")\n                \n        except KeyboardInterrupt:\n            engine.stop_engine()\n            \n            # Save session data\n            timestamp = datetime.now().strftime(\"%Y%m%d_%H%M%S\")\n            session_data = {\n                \"session_id\": timestamp,\n                \"engine_status\": engine.get_engine_status(),\n                \"positions\": [asdict(p) for p in engine.positions.values()],\n                \"learning_events\": [asdict(e) for e in engine.learning_events],\n                \"magic_intelligence\": engine.magic_intelligence\n            }\n            \n            filename = f\"magic_engine_session_{timestamp}.json\"\n            with open(filename, 'w') as f:\n                json.dump(session_data, f, indent=2, default=str)\n            \n            print(f\"💾 Session data saved to {filename}\")\n    \n    else:\n        print(\"❌ Failed to start engine\")\n\nif __name__ == \"__main__\":\n    main()
